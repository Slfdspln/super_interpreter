#!/bin/bash

# Super Interpreter launcher script - Enhanced with full automation
# Redirects to cristal-clean for full automation capabilities

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "[Cristal] Starting Enhanced Super Interpreter with Full Automation..."
echo "🚀 Loading: Calculator automation + Universal app control + Document creation"

# Run the enhanced launcher
./cristal-clean