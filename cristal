#!/bin/bash

# Super Interpreter launcher script - Enhanced with full automation
# Uses run.py for complete macOS automation capabilities

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "[Cristal] Starting Cristal Super Interpreter..."
echo "🚀 Loading: Docker automation + Calculator + Universal app control + Document creation"

# Run the main interpreter with all automation controllers
python3 run.py